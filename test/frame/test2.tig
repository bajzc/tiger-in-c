let
  var ret1 := 0
  function test_arg(a: int, b: int, c: int, d: int, e: int, f: int, g: int): int =
    let
      var ret2 := e + f + g
    in
      ret1 := a + b + c + d ; ret1 + ret2
    end
in
  ()
end